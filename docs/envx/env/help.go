package env

const Description string = "Work with envx environments"

const DescriptionList string = "List all envx environments"

const DescriptionShow string = "Print contents of envx environment to stdout"

const DescriptionNew string = "Create a new and empty envx environment"

const DescriptionDelete string = "Delete an envx environment"

const DescriptionUpdate string = "Update a value inside an envx environment"
